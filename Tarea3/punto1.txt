SELECTION SORT PSEUDO CODE

temp;

for (int n = 0; n < size - 1; ++n){
	temp = n;
	for (int m = 0; m < size) {
		if (lista[temp] < lista[m])
			temp = m;
	}
	swap (lista[temp], lista [n]);
}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

BINARY SEARCH PSEUDO CODE

array[n];
min = 0;
max = n;
flag = 0;

while (flag == 0) {
  if(array[(min + max)/2] == num)
    flag = 1
  else if((min + max)/2 > NUM)
    min == (min + max)/2
  else if((min + max)/2 < NUM)
    min == (min + max)/2
}

return 0;

